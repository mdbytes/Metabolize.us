import TermsPage from '../../_assets/components/terms/TermsPage';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Terms | Primal Strength and Conditioning',
};

export default function Terms() {
  return <TermsPage />;
}
